function start()
    -- juice.routine.create(function()
    --     entity.physics_circle.on_collision_start = function(body1, body2)
    --         if body2.scripts.circle_visual.can_capture then
    --             --entity.parent.scripts.bunny.capture()
    --         end
    --     end
    -- end)
end